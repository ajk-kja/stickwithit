name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull latest
        run: |
          cd /home/clank/infra/stickwithit
          git pull origin main
          
      - name: Restart container
        run: |
          cd /home/clank/infra/stickwithit
          docker compose restart
          echo "✅ Deployed at $(date)"
