name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull latest
        run: |
          cd /home/clank/projects/stickwithit
          git pull origin main

      - name: Normalize static file permissions
        run: |
          cd /home/clank/projects/stickwithit
          chmod -R a+rX www
          
      - name: Restart container
        run: |
          cd /home/clank/projects/stickwithit
          docker compose restart
          echo "✅ Deployed at $(date)"
